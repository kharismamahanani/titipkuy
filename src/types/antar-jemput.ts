export interface AntarJemputOption {
  id: string;
  label: string;
  tipe: string;
  radiusLabel: string;
  harga: number;
  kapasitasLabel: string | null;
  tipeArmada: string | null;
  aktif: boolean;
  urutan: number;
}
